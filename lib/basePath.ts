/**
 * Muss mit `basePath` in next.config.ts übereinstimmen. next/image hängt den
 * basePath (anders als next/link) nicht automatisch an `src` an, siehe
 * node_modules/next/dist/docs/.../basePath.md -> Abschnitt "Images".
 */
export const basePath = "/website";
